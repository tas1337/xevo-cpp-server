version: '3'
services:
  xevo-api:
    build: 
      context: .  # Points to project-root
      dockerfile: CombinedDockerfile 
    ports:
      - "3000:3000"
    depends_on:
      - xevo-cpp-server
  xevo-cpp-server:
    build: ./xevo-cpp-server
    ports:
      - "8080:8080"